{
    "configurations": [
        {
            "name": "Launch file",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/server/main.go",
            "env": {
                "CONFIG_PATH": "${workspaceFolder}/configuration/server"
            }
        }
    ]
}